'''
#집합1
aset= {1,2,3,4}
bset ={2,4,6,7}

# cset = aset.union(bset) #합집합 만들기
# cset = aset | bset  #shift 역슬래시 
# cset = bset | aset
# cset = bset.union(aset)
# -> 위에 4개 모두 같은 으ㅣ미


# cset = aset & bset
# cset = aset.intersection(bset) -> 위에랑 같은 의미
# cset = bset & aset
# cset = bset.intersection(aset)
# -> 위에 4개 모두 같은 으ㅣ미

print(cset)

# aset - bset 는 bset - aset와 같지 않음 -> 다른 의미임
'''











